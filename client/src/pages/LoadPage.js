const LoadPage = () => {
  return (
    <div>
      <h2>You can use the park table to search all the national parks in the united states</h2>
      <p>The app is loading. Once the loading is complete, it should run as normal with no more wait times.</p>
    </div>
  );
};

export default LoadPage;
